<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Added 21 August 2026 with firebase_messaging.

      REQUIRED ON ANDROID 13 (API 33) AND ABOVE. Without this line the runtime
      permission request returns "denied" immediately, with no dialog and no
      error — the host is never asked, never notified, and nothing anywhere
      says why. Older Android grants notifications at install time and ignores
      this declaration, so it is safe on every version.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ⚠ ADDED 24 August 2026, REQUIRED BY THE LIVE KYC PREVIEW.

         Until now this app only used image_picker, which hands the job to the
         phone's own camera app and therefore needs no permission of its own —
         which is why this line was never here.

         The camera package is different: it opens the hardware in-process for
         the auto-capture and liveness preview, and without this it throws
         CameraAccessDenied at the moment the selfie step opens.

         android:required="false" so the app still installs on a device with no
         camera. The selfie step needs one; the rest of the app does not. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:label="GoOuts Host"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
            ML Kit face model.

            NOTE: this is belt-and-braces, not the thing that makes it work.
            The google_mlkit_face_detection plugin depends on the BUNDLED
            artifact (com.google.mlkit:face-detection), which ships the model
            inside the app already — so face checks work offline and on first
            launch with or without this line.

            It is kept because it costs nothing and becomes load-bearing the
            day anyone switches to the thin Play-Services variant
            (play-services-mlkit-face-detection), which downloads the model on
            first use unless this tells Play Services to fetch it at install.
        -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="face" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
